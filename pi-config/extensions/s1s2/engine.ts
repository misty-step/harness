// Repo shim for the shared System One engine, like continuation-nudge/engine.ts.
// s1s2 runs from a repository checkout (run.sh), so it is never materialized.
export * from "../../../agent-config/system-one/engine.ts";
