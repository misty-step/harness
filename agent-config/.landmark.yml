product:
  name: agent-config
  description: Shared harness-neutral agent primitives for Misty Step harnesses. Source of truth for portable skills, shared global guidance, and the pass-env secret launcher.
audience: general
voice: clear, specific, user-facing
changelog:
  source: auto
artifacts:
  markdown: docs/releases/{version}.md
  json: docs/releases/releases.json
release:
  profile: full
model:
  policy: balanced
budget:
  max_input_tokens: 12000
  max_output_tokens: 1200
