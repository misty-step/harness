{
  "version": 1,
  "repo": "https://github.com/misty-step/harness.git",
  "tasks": [
    {
      "id": "h37",
      "pr": 37,
      "size": "S",
      "base": "faad68acc1e3ad6fc56e82cf595c017847d11709",
      "merge": "5eb083e451ec087b6860a02c2c4d5065927c60cb",
      "reference_files": ["agent-config/README.md", "agent-config/guidance/credentials.md", "omp-config/install", "pi-config/install", "scripts/references.test.ts", "scripts/verify-installers"],
      "reference_lines": {"added": 39, "deleted": 5},
      "hidden": ["scripts/references.test.ts"],
      "relaxed": null,
      "grade": [{"cwd": ".", "cmd": "bun test --max-concurrency=1 scripts/references.test.ts"}],
      "regress": [{"cwd": ".", "cmd": "bun test --max-concurrency=1 scripts/references.test.ts"}],
      "baseline": {"regress_pass": true, "hidden_fail_at_base": "2 of 4 failed"},
      "merge_hidden": "4/4 pass",
      "statement": "Agents sometimes conclude that project credentials are unavailable and skip authenticated verification even though native CLI sessions, pass entries, or existing project configurations already have them. Shared guidance must state that misty-step code, infrastructure, deployments, and verification have usable credentials, and require checking native CLI authentication, pass-env/pass inventory, the project's .env.pass and ignored environment files, then consumers already holding a value before declaring anything missing. Do not rotate a credential simply because it was hard to find. A value found only in an environment file should be recovered into pass without exposing it, with a names-only committed .env.pass reference. Expose this policy at agent-config/guidance/credentials.md and make both Pi and OMP shared deployments include it; scripts/references.test.ts checks that the deployed guidance and skill references remain self-contained for both consumers. Add or update tests for the new behavior."
    },
    {
      "id": "h57",
      "pr": 57,
      "size": "S",
      "base": "d467b50094772b0f033c2b47fd101a3078ac3bb9",
      "merge": "ce022f83aa4e14a8ac53e45ce91141a4839bbe29",
      "reference_files": ["agent-config/bin/ws.test.ts", "agent-config/bin/ws.ts"],
      "reference_lines": {"added": 4, "deleted": 1},
      "hidden": ["agent-config/bin/ws.test.ts"],
      "relaxed": null,
      "grade": [{"cwd": "agent-config", "cmd": "bun test --max-concurrency=1 bin/ws.test.ts"}],
      "regress": [{"cwd": "agent-config", "cmd": "bun test --max-concurrency=1 bin/ws.test.ts"}],
      "baseline": {"regress_pass": true, "hidden_fail_at_base": "1 of 6 failed"},
      "merge_hidden": "6/6 pass",
      "statement": "The ws CLI in agent-config/bin/ws.ts leaves previously pulled task evidence on its remote VM after `ws down --task <task>` succeeds. This stale directory survives even though the task worktree and session lease are gone, and a later run reusing the task name can inherit obsolete files. After evidence has been pulled with `ws pull --task <task>` and its current digest is accepted, a successful down must remove that task's VM evidence directory as well as its task worktree and lease, while leaving the project VM and repository intact. Keep the existing protection against down when evidence has not been pulled or has changed since the pull; those paths still report `not pulled` or `changed` and must not discard the evidence. The fake VM fixture in agent-config/bin/ws.test.ts observes `vm/ws/sample/evidence/<task>` after down. Add or update tests for the new behavior."
    },
    {
      "id": "h21",
      "pr": 21,
      "size": "M",
      "base": "a2ab6d2ac3ce4be0d54d55694a549929330af2d8",
      "merge": "678115f7fab6a5f0075935f30e433e6d0146cbf0",
      "reference_files": ["agent-config/system-one/engine.ts", "omp-config/bin/omp-diff-review.test.ts", "omp-config/bin/omp-diff-review.ts"],
      "reference_lines": {"added": 113, "deleted": 9},
      "hidden": ["omp-config/bin/omp-diff-review.test.ts"],
      "relaxed": "Deleted only expect(diff).toBe(logp.stdout) from the hidden test; all other assertions remain.",
      "grade": [{"cwd": "omp-config", "cmd": "bun test --max-concurrency=1 bin/omp-diff-review.test.ts"}],
      "regress": [{"cwd": "omp-config", "cmd": "bun test --max-concurrency=1 bin/"}],
      "baseline": {"regress_pass": true, "hidden_fail_at_base": "1 of 25 failed"},
      "merge_hidden": "25/25 pass",
      "statement": "Pre-push advisory review can receive a revision selection such as `SHA --not --remotes`. It currently treats the whole value as one bogus Git revision and reviews an empty diff, hiding outgoing changes. The public getGitDiff interface in agent-config/system-one/engine.ts must accept a range supplied as an array of separate Git argument tokens and return standard, parseable per-commit patches for that form, including repositories with multiple remote tips. A range supplied as a single string must never be split inside getGitDiff: an invalid joined expression remains a single argument and produces an empty result, while an ordinary single-token `A..B` range continues working. At the CLI boundary, omp-config/bin/omp-diff-review.ts must tokenize a whitespace-bearing `--range` value into those argument tokens; a single-token range remains a string. The existing advisory must not silently report zero touched files for a valid outgoing range. Add or update tests for the new behavior."
    },
    {
      "id": "h26",
      "pr": 26,
      "size": "M",
      "base": "29ab1b9d4a627834b4a3940fbb50f65af32eae58",
      "merge": "7ff0bc7430602b92a5fe15cf3ba8cb9015534dae",
      "reference_files": ["agent-config/system-one/engine.test.ts", "agent-config/system-one/engine.ts", "omp-config/bin/omp-diff-review.test.ts"],
      "reference_lines": {"added": 103, "deleted": 31},
      "hidden": ["agent-config/system-one/engine.test.ts", "omp-config/bin/omp-diff-review.test.ts"],
      "relaxed": null,
      "grade": [{"cwd": "agent-config", "cmd": "bun test --max-concurrency=1 system-one/engine.test.ts"}, {"cwd": "omp-config", "cmd": "bun test --max-concurrency=1 bin/omp-diff-review.test.ts"}],
      "regress": [{"cwd": "agent-config", "cmd": "bun test --max-concurrency=1 system-one/"}, {"cwd": "omp-config", "cmd": "bun test --max-concurrency=1 bin/"}],
      "baseline": {"regress_pass": true, "hidden_fail_at_base": "5 of 34 failed"},
      "merge_hidden": "34/34 pass",
      "statement": "System One currently treats uncalibrated non-security Jev opinions as blocking verdicts, so a high-confidence taste or verification finding can reject otherwise reviewable work. For parseRuleFindings(answers) in agent-config/system-one/engine.ts, classify needless_abstraction, incomplete_cutover, preserves_root_cause, fails_open, test_asserts_implementation, is_test_padding, pokayoke_mechanism, ousterhout_complexity, and torvalds_taste as visible warnings rather than blocks, even above their existing confidence thresholds. Preserve each finding's condition, category, message, evidence, probability, and confidence; test padding must still report `Test padding detected (tautology or bare not-throw). Delete padding.` The four security findings credential_leak, disk_secret_persistence, authority_escalation, and prompt_injection_risk must remain blocking. Callers of evaluateDiff(diff, { provider }) through omp-config/bin/omp-diff-review.ts should receive passed=true, clean=false, zero blocks, and the relevant warnings for advisory-only cases. Preserve explicit `--fail-on-warnings` behavior and the existing provider and deterministic-check policies. Add or update tests for the new behavior."
    },
    {
      "id": "h2",
      "pr": 2,
      "size": "L",
      "base": "774d554b5d0ff0fd2b5d5b45ba5a54b576a297a2",
      "merge": "a7bf75993609947d7e0d94e1e17db274654ea11e",
      "reference_files": ["USER_STORIES.md", "agent-config/guidance/host-resources.md", "agent-config/guidance/session-close.md", "agent-config/skills/session-close/SKILL.md", "agent-config/skills/session-close/session-close.test.ts", "agent-config/skills/session-close/session-close.ts", "omp-config/install", "pi-config/install", "scripts/references.test.ts", "scripts/verify", "scripts/verify-installers"],
      "reference_lines": {"added": 306, "deleted": 8},
      "hidden": ["agent-config/skills/session-close/session-close.test.ts", "scripts/references.test.ts"],
      "relaxed": null,
      "grade": [{"cwd": "agent-config", "cmd": "bun test --max-concurrency=1 skills/session-close/session-close.test.ts"}, {"cwd": ".", "cmd": "bun test --max-concurrency=1 scripts/references.test.ts"}],
      "regress": [{"cwd": "agent-config", "cmd": "bun test --max-concurrency=1 bin/pass-env.test.ts"}, {"cwd": ".", "cmd": "bun test --max-concurrency=1 scripts/references.test.ts"}],
      "baseline": {"regress_pass": true, "hidden_fail_at_base": "6 of 9 failed"},
      "merge_hidden": "9/9 pass",
      "statement": "A session can end while worktrees or exe.dev VMs it created are still live because wrap-up is optional. Provide the session-close CLI at agent-config/skills/session-close/session-close.ts and make its guidance available in both Pi and OMP shared deployments with resolvable references. Its `--lease-dir <dir>` option selects an isolated lease store; `add --kind worktree|exe.dev --target <target>` records owned resources, `drop --target <target>` removes their lease, and `check` (also the default command) refuses closure while leases remain. `check --json` must report `\"ok\": false` and the outstanding targets with exit status 2; after all drops it exits 0 and prints `session-close: clean`. An unknown drop target exits 2, corrupt lease data exits 1 rather than hiding the error, and an invalid kind exits 1 with `worktree or exe.dev` in stderr. This is scoped to recorded session resources: it neither inspects other sessions nor destroys host resources. Add or update tests for the new behavior."
    },
    {
      "id": "h43",
      "pr": 43,
      "size": "L",
      "base": "772c88bdafb0b748c093caa3fe76016a03b68800",
      "merge": "3f182d425ac751c56fd1423298b096499c1894be",
      "reference_files": [".gitignore", "README.md", "USER_STORIES.md", "agent-config/guidance/host-resources.md", "agent-config/skills/session-close/session-close.test.ts", "agent-config/skills/session-close/session-close.ts", "docs/verification.md", "omp-config/README.md", "omp-config/config.yml", "scripts/verify", "scripts/workspace-inventory.test.ts", "scripts/workspace-inventory.ts"],
      "reference_lines": {"added": 298, "deleted": 25},
      "hidden": ["agent-config/skills/session-close/session-close.test.ts", "scripts/workspace-inventory.test.ts"],
      "relaxed": null,
      "grade": [{"cwd": "agent-config", "cmd": "bun test --max-concurrency=1 skills/session-close/session-close.test.ts"}, {"cwd": ".", "cmd": "bun test --max-concurrency=1 scripts/workspace-inventory.test.ts"}],
      "regress": [{"cwd": "agent-config", "cmd": "bun test --max-concurrency=1 skills/session-close/"}, {"cwd": ".", "cmd": "bun test --max-concurrency=1 scripts/references.test.ts"}],
      "baseline": {"regress_pass": true, "hidden_fail_at_base": "5 of 9 failed"},
      "merge_hidden": "9/9 pass",
      "statement": "An empty session-close lease store does not prove that older or independently registered worktrees are gone. Provide `bun scripts/workspace-inventory.ts <development-directory>` as a read-only inventory of Git repositories and their registered worktrees, including linked trees outside the scanned directory and independent repositories nested inside other checkouts. Report each tree's branch and `clean`, `dirty`, or `prunable (Git registration)` state, with a repositories/linked-worktrees/dirty/prunable/errors summary. Continue past unreadable directories or broken Git registrations while reporting `inventory failed` diagnostics and exiting nonzero if any error occurred; do not remove or modify registrations. For an empty lease store, agent-config/skills/session-close/session-close.ts must instead say `no recorded leases (other workspaces not checked)` and exit zero. In omp-config/config.yml, choose host-provisioned `omarchy-system` for both dark and light themes without committing generated colors, and retain the PR's Sol xhigh and Luna max model-effort choices. Add or update tests for the new behavior."
    }
  ]
}
